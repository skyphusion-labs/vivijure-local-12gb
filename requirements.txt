# vivijure-local-backend runtime stack (the 16GB consumer door). LTX-Video image-to-video on a single
# RTX 4060 Ti-class card, plus R2 object I/O. torch/torchvision are installed from the CUDA index in
# deploy/Dockerfile (matched to the card's CUDA line), NOT pinned here. These are the SCAFFOLD pins;
# the exact validated version set is fixed during the card benchmark (docs/live-benchmark-plan.md).

# --- LTX-Video i2v engine (HuggingFace diffusers) ---
diffusers>=0.32.0          # LTXImageToVideoPipeline (first-class LTX support)
transformers>=4.44.0
accelerate>=0.34.0         # enable_model_cpu_offload / enable_sequential_cpu_offload (the 16GB savers)
safetensors>=0.4.5
sentencepiece>=0.2.0       # LTX text encoder tokenizer
imageio>=2.34.0
imageio-ffmpeg>=0.5.1      # export_to_video
av>=12.0.0                 # frame-accurate clip write

# --- object storage (shared vivijure R2 bucket) ---
boto3>=1.34.0
