# NOTE (S8 re-pin): the ML stack was RE-PINNED and re-proved on real silicon -- diffusers 0.38.0 +
# transformers 4.57.6 on torch 2.5.1 (cu124), accelerate 1.14.0 / safetensors 0.8.0. See SECURITY.md
# for the advisory paper trail. All deps below are pinned to exact, validated versions for reproducible builds.

# vivijure-local-12gb runtime stack (the 12GB consumer door). LTX-Video image-to-video on a single
# 12GB-class consumer card, plus R2 object I/O. torch/torchvision are installed from the CUDA index in
# deploy/Dockerfile (matched to the card's CUDA line). diffusers/transformers are PINNED to the exact
# set validated on real silicon in the S8 re-pin proof: diffusers 0.38.0 on torch 2.5.1 (cu124), with
# transformers pinned to the latest 4.x (4.57.6) -- diffusers 0.38 is not built against transformers 5.x,
# which also needs a new tiktoken dependency, so we stay on 4.x. See SECURITY.md for the paper trail.

# --- LTX-Video i2v engine (HuggingFace diffusers) ---
diffusers==0.38.0          # validated LTX release (LTXImageToVideoPipeline) on torch 2.5.1 (S8 re-pin proof)
transformers==4.57.6       # latest 4.x; validated with diffusers 0.38.0 on torch 2.5.1 (5.x breaks the T5 tokenizer load)
accelerate==1.14.0         # enable_model_cpu_offload / enable_sequential_cpu_offload (the 12GB savers)
safetensors==0.8.0
sentencepiece==0.2.2       # LTX text encoder tokenizer
imageio==2.37.3
imageio-ffmpeg==0.6.0      # export_to_video
av==17.1.0                 # frame-accurate clip write. DO NOT bump to 18.x: av 18 drops Python 3.10
                           # wheels and the door image builds on py3.10, so the bump breaks the GHCR
                           # publish (the only Docker build; CI never catches it). Bump only with a
                           # base-image Python move. Dependabot ignore below.

# --- object storage (shared vivijure R2 bucket) ---
boto3==1.43.51
